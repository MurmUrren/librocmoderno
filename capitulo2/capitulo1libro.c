#include <stdio.h> //esto se le conoce como directivo, son comandos ejecutados antes del resto, stdio significa 'standard io', es una libreria

int main(void)
{
    int prueba; // declaracion de variable de tipo int o entero
    prueba = 1; // asignacion de valor a una variable

    printf("El valor de prueba es: %d\n", prueba); // statement, es una instruccion, un comando a ejecutar cuando el programa este corriendo.

    return 0; // return tambien es un statement
}